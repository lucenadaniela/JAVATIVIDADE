faça um programa que verifique (usando if e else) se uma letra digitada é "F" ou "M". Conforme a letra escrever F - Ferminino, M - Masculinoimport java.util.Scanner;

public class VerificaGenero {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Solicita ao usuário que digite uma letra
        System.out.print("Digite uma letra (F/M): ");
        String letra = scanner.next();

        // Converte a letra para maiúscula para evitar diferenças de maiúsculas/minúsculas
        letra = letra.toUpperCase();

        // Verifica se a letra é "F" ou "M" e imprime a mensagem correspondente
        if (letra.equals("F")) {
            System.out.println("F - Feminino");
        } else if (letra.equals("M")) {
            System.out.println("M - Masculino");
        } else {
            System.out.println("Letra inválida. Por favor, digite apenas 'F' ou 'M'.");
        }

        scanner.close();
    }
}
